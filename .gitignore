# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader
# Transpiled JS artifacts (emitted when a contributor runs `tsc` manually
# instead of `docusaurus build`, which uses Babel and never emits .js into src/).
docusaurus.config.js
sidebars.js
*.js.map
src/**/*.js

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
